plugins {
    alias(catalog.plugins.envelope)
}

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':')
}

envelopeJar {
    mainClass = 'net.woggioni.jwo.benchmark.Main'
}

